import ProfileContainer from "../ProfileContainer";
import styles from "./ClaimDetails.module.css";

const ClaimDetails = () => {
  return (
    <ProfileContainer>Your submitted claims will appear here.</ProfileContainer>
  );
};

export default ClaimDetails;
